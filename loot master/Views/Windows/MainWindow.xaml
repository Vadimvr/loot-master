<?xml version="1.0" encoding="utf-8" ?>
<Window xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
        x:Class="loot_master.Views.Windows.MainWindow"
        xmlns:p="clr-namespace:loot_master.Views.Pages"
        xmlns:c="clr-namespace:loot_master.Views.Controls"
        BindingContext="{Binding Path=MainWindowViewModel, Source={StaticResource Locator}}"
        Title="{Binding Title}">
    <Shell>
        <TabBar>
            <Tab Title="Guid">
                <ShellContent ContentTemplate="{DataTemplate p:MainPage}" />
            </Tab>
            <Tab Title="Raid">
                <ShellContent ContentTemplate="{DataTemplate p:MainPage}" />
            </Tab>
        </TabBar>
        <!--<FlyoutItem Title="Guild_1">
            <ShellContent  ContentTemplate="{DataTemplate p:MainPage}" />
        </FlyoutItem>
        <FlyoutItem Title="Raid">
            <ShellContent Route="OnePage">
                <ContentPage>
                    <ScrollView VerticalScrollBarVisibility="Always"
                                Margin="4"
                                Padding="0,0,0,0">
                        <StackLayout VerticalOptions="Fill"
                                     HorizontalOptions="Fill">
                            <c:RaidView />
                            <c:LogWinnerView />
                        </StackLayout>
                    </ScrollView>
                </ContentPage>
            </ShellContent>
        </FlyoutItem>-->
    </Shell>
</Window>